# 服务器数据只读核查（2026-09-09）

核查位置：服务器 `/root/autodl-tmp/ANGA`。未修改服务器文件或启动训练。
本地与服务器 HEAD 均为 `cdc2c3f773fe3da499ed6254c5384a32249d46a2`。
以下为当前服务器预处理文件的统计；未找到逐实验归档的数据/掩码快照，不能独立证明每次历史运行的数据字节完全一致。

## 数据划分

train/valid/test 分别为 8500/500/1000 条，item_id 在各划分内唯一，划分之间 ID 交集为零。
正类数量分别为 3050/250/490。

## 验证样本检索到自身

| 检索方向 | Top-1 | Top-5 | Top-9 |
|---|---:|---:|---:|
| i2i | 491/500 | 499/500 | 500/500 |
| t2t | 400/500 | 499/500 | 499/500 |

70% 单模态缺失掩码下，验证集有 346 条缺失样本。
Text 场景中，其中 341/346 条在 i2i Top-1 检索到自身，346/346 条在 Top-5 检索到自身。
Image 场景中，对应 t2t 数量为 276/346、345/346。
所有划分、两种检索方向的已存储邻居标签均与邻居 ID 的真实标签一致（不匹配数为 0）。

代码将邻居模态特征用于补全，并将邻居标签用于 label_enhanced 提示。因此这些自身邻居提供了本应不可用的自身模态信息及标签。

## 训练与测试检索使用验证集

Top-5 共 42500 个训练邻居位置中，i2i 有 1906 个来自 valid，t2t 有 2050 个来自 valid。
Top-5 共 5000 个测试邻居位置中，i2i 有 364 个来自 valid，t2t 有 332 个来自 valid。
检查的 Top-1/5/9 邻居中，没有来自 test 的邻居；train/test 自身检索数为零。
这确认验证集隔离被破坏，但不代表测试集自身标签进入记忆库。

根因：core_tools.py 的 MCR 使用 train+valid 构建库；排除自身时以 memory_bank_id[query_index] 代替真实查询 ID，仅在训练查询顺序匹配时正确。

## 缺失掩码

single/both 掩码表均为 10000 个唯一 ID，合并后各划分行数不变。
single 70% 实际缺失数：train 5934/8500、valid 346/500、test 720/1000。
both 70% 的 mask=0/1/2 数量：train 2963/2983/2554、valid 174/188/138、test 363/329/308。
这是全数据集合计采样的结果，各划分缺失率不严格等于 70%，不是合并丢失样本。

## 当前文件 SHA-256

| 文件 | SHA-256 |
|---|---|
| train.pkl | f3c417a1b41fbcb52b8e6d79bd09dd8ab2ac1b31fdecb32e5bb8407c3de74cb5 |
| valid.pkl | 44d723a5aa6fdbeceb2aacb74ab6c3374739817afab2d532b639182f9e7504a2 |
| test.pkl | 2c32b27df1bdfac7eae98dd5b1372058b536c4ec918972c97c14893ed384f18c |
| single missing_table.pkl | a60fdd1e413f924851d73dec8f9ed0504565341938953af1dcb76dd34e554f9e |
| both missing_table.pkl | 57ecdc1e93cb90106f2cbf7828678a21dab7a509bf4575994e7c78f8c6e50d89 |

## 结论与后续

现有结果应保留为原实现实验，不能作为独立验证协议下的论文复现证据。K=1 的验证/测试差距与此问题相容，但尚未量化其因果贡献。
优先建立仅含训练样本的检索库，使用真实查询 ID 排除自身，重新生成全部划分的邻居 ID/标签/相似度并检查对齐；明确库是否允许包含训练样本被模拟遮蔽的模态。随后与注意力掩码等实现修正分开记录，重新训练及选择 checkpoint。
仅替换检索表后评估旧 checkpoint 可用于诊断，不能替代干净流程下的重训，因为历史训练已经使用验证集信息。

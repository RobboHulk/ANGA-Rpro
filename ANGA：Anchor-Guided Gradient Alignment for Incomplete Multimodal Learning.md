### Anchor-Guided Gradient Alignment for Incomplete Multimodal Learning

### 面向不完全多模态学习的锚点引导梯度对齐

------

- **期刊：** CVPR 2026
- **类别：** 不完全多模态学习、缺失模态、检索式模态重建、优化平衡与梯度对齐、提示学习
- **一段话总结：** 视觉-语言预训练（VLP）在多模态学习中表现优异，近期大量工作通过**重建缺失模态**来提升VLP模型在不完全多模态场景下的适应性。但这些方法忽视了一个关键现象：**在高缺失率下，携带语义噪声的重建样本会主导优化过程，从而削弱完整样本学到的表示，形成"学习不平衡"。** 论文在HateMemes上以70%文本缺失做了一个简易实验：完整样本的性能明显低于其独立训练所得的上界，而重建样本却仍保持竞争力，二者之间出现巨大差距。为此，作者提出**锚点引导梯度对齐（ANchor-guided Gradient Alignment，ANGA）**：先用多模态实例检索器（MIR）从记忆库中检索**Top-K相似实例**重建缺失模态以缓解信息缺失；再用**熵驱动课程**把置信度高（预测熵低）的可靠重建样本逐步与完整样本合并，构成**优化锚点**；随后以锚点梯度为方向参考，对不在锚点内的重建梯度做**锥形区域内的三分支调制**（锥内保留、锥外但正相关则投影到锥边界、与锚点相反则置零），从而抑制噪声引起的优化漂移；此外设计**语义增强适配器（SEA）**，用检索实例通过交叉注意力生成动态、上下文感知的提示注入MSA层。在HateMemes、MM-IMDb、Food101三个数据集及多种缺失场景下，ANGA均优于模态不变学习、基于VLP和模态重建三类SOTA基线。
- **文章链接：** https://openaccess.thecvf.com/CVPR2026?day=all
- **源码链接：** https://github.com/guanzh1997/CVPR26-ANGA

---

### 1. 研究背景与动机

#### 1.1 要解决的问题

多模态学习通过整合多种模态的互补信息提升性能，但**大多数现有方法假设训练和推理阶段所有模态都完全可用**。实践中由于传感器故障、传输错误或隐私约束，不完整数据普遍存在。

针对缺失模态问题，现有工作大致可分为三类：

- **模态不变学习方法：** 强制实现跨模态语义一致性，将多模态特征映射到共享语义空间。但往往**忽视模态特定线索**，导致性能欠佳。
- **基于VLP的方法：** 利用大规模预训练 + 提示微调迁移跨模态知识。但提示通常是**静态、输入无关**的，上下文知识有限，在混合缺失场景下适应性受限。
- **模态重建方法：** 通过生成式或检索式策略恢复缺失模态。得益于检索增强与跨模态生成技术的发展，这类方法已成为不完全多模态学习的主流。

论文指出，模态重建方法忽视了一个重要问题：

> **不完全多模态学习在高缺失率下会遭受"学习不平衡"（learning imbalance）。**
>
> 重建样本携带被放大的语义噪声，却倾向于**主导优化过程**，使模型更新产生偏置，削弱了从完整样本中学到的表示。

#### 1.2 动机实验

作者在**HateMemes**数据集上、**70%文本缺失**条件下做了一个简易（toy）实验，按通用检索式重建范式**（RAGPT）**从记忆库中检索语义相似实例来重建缺失模态，分别考察完整样本与重建样本的性能。

<img src="./assets/ANGA-fig1.png" alt="ANGA-fig1" style="zoom: 50%;" />

结果揭示了一个显著现象：

- 理想情况下，两组样本都应接近**各自的上界**（通过在各子集上独立训练得到）；

- 实际上，**完整样本的性能明显低于其上界**，而**重建样本仍保持相对竞争力**；

  > **模型的优化被重建样本偏置**

- 二者之间存在**巨大性能差距（Large Gap）**。

这证实了学习不平衡的存在，并促使作者设计一个**平衡优化框架**来缩小该差距。

#### 1.3 相关研究

##### （1）不完全多模态学习

不完全多模态学习旨在某些模态缺失时仍实现鲁棒预测，现有研究主要沿三条路线：

- **模态不变学习：** 提取模态间相关性，将多模态特征映射到共享语义空间，增强缺失条件下的鲁棒性（如IF-MMIN [56]、ShaSpec [32]、CorrKD [18]）。
- **大规模预训练 + 提示微调：** 将跨模态知识从VLP模型迁移到不完全多模态任务（如MAPs [16]、MSPs [11]、DPL [45]）。
- **模态重建：** 通过**生成式**（如DiCMoR [37]、AcMAE [40]）或**基于检索**（如RAGPT [15]、MMDS [47]）的策略恢复缺失信息，以提升联合表示质量。

但重建样本中的**语义噪声可能导致优化漂移**，进而引发学习不平衡。ANGA正是针对这一点，提出锚点引导的梯度对齐策略来稳定完整样本与重建样本之间的优化。

##### （2）学习不平衡

论文把"学习不平衡"视为一个比"模态不平衡"更广的概念，包含隐式与显式两种形式：

- **隐式形式（完全多模态场景）：** <u>收敛速度较快的模态主导优化过程</u>，导致**模态不平衡**。相关工作从学习目标 [38,48]、架构设计 [3]、优化策略 [26,50] 等角度缓解。
- **显式形式（不完全多模态场景）：** 缺失模态的**重建过程引入语义噪声**，放大优化漂移，导致学习不平衡，也本文的主要关注点。

作者强调：以往工作主要平衡"完全设置下的模态贡献"，而**本文是首次从优化视角研究缺失模态条件下的学习不平衡**。

#### 1.4 主要贡献

- **识别并分析**了现有模态重建方法中的学习不平衡问题，指出其显著降低整体性能；
- 提出**ANGA**，通过梯度对齐重新平衡完整样本与重建样本之间的优化，且**可无缝集成到现有不完全多模态学习框架**；
- 在广泛使用的基准数据集上，ANGA**持续优于SOTA基线**。



### 2. 预备知识

#### 2.1 问题定义

不失一般性，考虑由两种模态 $$t$$ 和 $$v$$（例如文本和图像）组成的多模态样本。令数据集为：

$$
\mathcal{D}=\{\mathcal{D}^{c},\mathcal{D}^{m}\}
$$

其中**完全模态子集**为：

$$
\mathcal{D}^{c}
=
\left\{
x_i^{(t)},x_i^{(v)},y_i
\right\}_{i=1}^{N^{c}}
$$

包含 $$N^{c}$$ 个训练样本，$$y_i\in\{0,1\}^{k}$$ 表示共 $$k$$ 个类别的类别标签。

**缺失模态子集**为：

$$
\mathcal{D}^{m}
=
\left\{
\left(x_i^{(t)},y_i\right)
\vee
\left(x_i^{(v)},y_i\right)
\right\}_{i=1}^{N^{m}}
$$

包含 $$N^{m}$$ 个**仅有一种模态可用**的训练样本。

不完全多模态学习的目标是：**在训练和推理阶段某些模态缺失的情况下，仍能实现鲁棒的多模态预测。**

#### 2.2 视觉-语言预训练（VLP）

VLP将视觉与文本表示对齐到共享嵌入空间。典型模型（如ViLT）先通过模态特定嵌入层将文本 $$x_i^{(t)}$$ 与图像 $$x_i^{(v)}$$ 映射为token序列：

$$
\mathcal{T}_i\in\mathbb{R}^{n_t\times d},
\qquad
\mathcal{V}_i\in\mathbb{R}^{n_v\times d}
$$

其中 $$n_t,n_v$$ 为序列长度，$$d$$ 为嵌入维度。拼接后的词元由包含 $$B$$ 个连续**多头自注意力（MSA）**层的多模态Transformer编码，生成联合表示 $$e_i\in\mathbb{R}^{d}$$，再送入分类器：

$$
e_i=\mathrm{Transformer}\left(\left[\mathcal{T}_i;\mathcal{V}_i\right]\right),
\qquad
\hat{y}_i=\sigma\left(We_i+b\right)
$$

其中 $$W\in\mathbb{R}^{k\times d}$$、$$b\in\mathbb{R}^{k}$$ 为分类器权重与偏置，$$\sigma$$ 为softmax。目标函数为交叉熵：

$$
\mathcal{L}_{ce}
=
-\frac{1}{N}
\sum_{i=1}^{N}
y_i^{\top}\log\hat{y}_i,
\qquad
N=N^{c}+N^{m}
$$

为高效适配下游任务，**提示微调**引入一小组 $$n_p$$ 个可学习提示 $$P\in\mathbb{R}^{n_p\times d}$$，在**冻结预训练主干**的同时实现任务特定适配。设 $$E_b\in\mathbb{R}^{L\times d}$$ 为第 $$b$$ 个MSA层的输入嵌入（$$E_1$$ 由拼接 $$\mathcal{T}_i$$ 与 $$\mathcal{V}_i$$ 形成），提示被前置为：

$$
E_b^{p}=\left[P;E_b\right]\in\mathbb{R}^{(n_p+L)\times d}
$$

送入第 $$b$$ 层并传播至后续层。为平衡效率与有效性，**提示仅应用于选定的层**（例如第 $$b$$ 层），而非插入每一层。

#### 2.3 模态重建范式：多模态实例检索器（MIR）

为处理不完整输入，先前工作常采用**多模态实例检索器（MIR）**重建缺失模态。为此构建**记忆库 $$\mathcal{M}$$ 存储单模态表示作为先验知识**。

以**缺失图像**为例：文本token序列 $$\mathcal{T}_i$$ 由CLIP文本编码器 $$\Phi^{(t)}(\cdot)$$ 编码得到文本嵌入：

$$
e_i^{(t)}=\Phi^{(t)}\left(\mathcal{T}_i\right)\in\mathbb{R}^{d}
$$

随后MIR以 $$e_i^{(t)}$$ 为查询，基于**余弦相似度**从 $$\mathcal{M}$$ 中检索最相似的Top-$$K$$个实例 $$\mathcal{N}_i$$：

$$
\mathcal{N}_i
=
\mathop{\text{Top-}K}\limits_{r\in\mathcal{M}}
\left(
\frac{
e_i^{(t)\top}e_r^{(t)}
}{
\left\|e_i^{(t)}\right\|_2
\left\|e_r^{(t)}\right\|_2
}
\right)
$$

其中 $$e_r^{(t)}$$ 是存储在 $$\mathcal{M}$$ 中的文本嵌入。$$\mathcal{N}_i$$ 中**对应的模态**作为语义参考，通过**平均池化**重建缺失模态。

对于**缺失文本**的情况，图像token序列 $$\mathcal{V}_i$$ 由CLIP视觉编码器 $$\Phi^{(v)}(\cdot)$$ 编码得到 $$e_i^{(v)}\in\mathbb{R}^{d}$$，检索过程完全相同。



### 3. 方法

#### 3.1 整体框架

![ANGA-fig2](./assets/ANGA-fig2.png)

ANGA由三部分构成：

1. **多模态实例检索器（MIR）：** 检索Top-$$K$$最相似实例以重建缺失模态；
2. **视觉-语言预训练（VLP）+ 语义增强适配器（SEA）：** 学习**联合表征**进行最终预测，SEA生成**动态提示**增强鲁棒性；
3. **梯度对齐（GA）：** 构建优化锚点，解决训练过程中的学习不平衡问题。

其中**锚点引导梯度对齐**负责平衡完整样本与重建样本之间的优化，**语义增强适配器**负责上下文语义丰富（因为仅靠优化层面的平衡无法完全解决语义不足问题）。

#### 3.2 优化锚点

##### 3.2.1 梯度解耦

在高缺失率下，噪声重建样本往往主导优化过程。为此，作者在**每个小批量** $$\mathcal{B}$$ 内**独立计算**完整样本与重建样本的梯度，从而解耦优化信号：

$$
g_{\mathcal{C}}
=
\nabla_{\theta}
\frac{1}{|\mathcal{C}|}
\sum_{i\in\mathcal{C}}\ell_i,
\qquad
g_{\mathcal{M}}
=
\nabla_{\theta}
\frac{1}{|\mathcal{M}|}
\sum_{i\in\mathcal{M}}\ell_i
$$

其中：

- $$\ell_i$$ 表示单样本损失（与交叉熵目标一致）；
- $$\nabla_{\theta}$$ 表示相对于模型参数的梯度；
- $$\mathcal{C}$$ 与 $$\mathcal{M}$$ 分别代表 $$\mathcal{B}$$ 中的**完全子集**与**重建子集**。

这种解耦使 $$g_{\mathcal{C}}$$ 能够作为**干净且基础的优化参考**。

##### 3.2.2 重建样本可靠性评估（预测熵）

若只用完整样本构建锚点，在高缺失率下锚点的**代表性不足**（完整样本本身就很少）。因此作者希望**引入一部分被认为可靠的重建样本**来丰富锚点，同时不牺牲稳定性。

对每个重建样本：

$$
\tilde{x}_i
=
\left\{\tilde{x}_i^{(t)},x_i^{(v)}\right\}
\ \ \text{或}\ \
\left\{x_i^{(t)},\tilde{x}_i^{(v)}\right\}
$$

使用**预测熵**评估其可靠性：

$$
H(\tilde{x}_i)
=
-\sum_{j=1}^{k}
\hat{y}_{i,j}\log\hat{y}_{i,j}
$$

其中 $$\hat{y}_{i,j}$$ 是样本 $$i$$ 属于类别 $$j$$ 的softmax概率。

> **熵较低的重建样本置信度更高，因此更受青睐用于锚点构建。**

##### 3.2.3 熵驱动课程与锚点构建

引入可靠重建样本可以丰富锚点，但**一次性全部合并**会因模型置信度仍在演变而引起不稳定。为确保平滑适应，作者采用**课程学习策略**调节引入的**数量与节奏**。

具体地，先按公式中的熵**升序**排列所有重建样本，得到 $$\tilde{\mathcal{D}}^{m}_{rank}$$；随后用一个广泛使用的**线性函数** $$\lambda(\cdot)$$ 实现该课程：

$$
\lambda(z)
=
\min
\left(
\lambda_{\max},\
\lambda_{\min}
+
\frac{\lambda_{\max}-\lambda_{\min}}{Z_{grow}}\cdot z
\right)
$$

其中：

- $$\lambda_{\min}$$、$$\lambda_{\max}$$ 分别为样本比例的**下界与上界**；
- $$Z_{grow}$$ 为**一个课程阶段中的epoch数量**；
- $$z$$ 为当前epoch。

在每个epoch $$z$$，$$\tilde{\mathcal{D}}^{m}_{rank}$$ 中排名**前 $$\lambda$$ 比例**的样本被逐步纳入锚点集：

$$
\mathcal{A}_z
=
\mathcal{C}
\cup
\left\{
\tilde{x}_i
\mid
\tilde{x}_i\in\tilde{\mathcal{D}}^{m}_{rank},\
i\leq\left\lfloor\lambda(z)\cdot N^{m}\right\rfloor
\right\}
$$

其中 $$\mathcal{C}$$ 表示**整个训练集上的完整模态子集**，$$N^{m}$$ 为重建样本总数。

最后，在每个小批量 $$\mathcal{B}$$ 内计算对应的**锚点梯度**：

$$
g_{\mathcal{A}}
=
\nabla_{\theta}
\frac{1}{\left|\mathcal{A}_z\cap\mathcal{B}\right|}
\sum_{i\in(\mathcal{A}_z\cap\mathcal{B})}\ell_i
$$

> 这种**批级（batch-wise）**优化锚点为后续的梯度对齐提供了**动态而稳定**的方向参考。

#### 3.3 梯度对齐

给定锚点 $$g_{\mathcal{A}}$$，将其作为**方向参考**，用于指导**不属于锚点集**的剩余重建梯度 $$g_{\mathcal{M}'}$$ 的优化，其中：

$$
\mathcal{M}'=\mathcal{M}-\left(\mathcal{A}_z\cap\mathcal{B}\right)
$$

对齐的总体思想为：**保留一致的梯度、将偏离的梯度投影到锚点方向、抑制冲突的梯度**，以防止噪声更新造成干扰。

形式上，对每个小批量 $$\mathcal{B}$$ 计算 $$g_{\mathcal{M}'}$$ 与 $$g_{\mathcal{A}}$$ 的**余弦相似度** $$\mathrm{sim}(g_{\mathcal{M}'},g_{\mathcal{A}})$$（公式与检索中的余弦相同，但作用于**梯度空间**）。基于该相似度，$$g_{\mathcal{M}'}$$ 依据其相对于锚点**锥形区域**的位置进行调制，锥形区域由余弦阈值定义：

$$
\tau=\cos\theta,
\qquad
\theta\in\left(0,\frac{\pi}{2}\right)
$$

共分三种情况：

##### （1）锥内：方向一致，原样保留

当 $$\mathrm{sim}(g_{\mathcal{M}'},g_{\mathcal{A}})\geq\tau$$ 时，梯度与锚点方向一致，不作修改：

$$
\tilde{g}_{\mathcal{M}'}\leftarrow g_{\mathcal{M}'}
$$

##### （2）锥外但相关：投影到锥边界

当 $$0<\mathrm{sim}(g_{\mathcal{M}'},g_{\mathcal{A}})<\tau$$ 时，梯度位于锥外但仍与锚点**正相关**。此时将 $$g_{\mathcal{M}'}$$ 分解为**平行分量**与**正交分量**：

$$
g_{\mathcal{M}'}^{\parallel}
=
\frac{g_{\mathcal{M}'}\cdot g_{\mathcal{A}}}
{\left\|g_{\mathcal{A}}\right\|_2^{2}}
g_{\mathcal{A}},
\qquad
g_{\mathcal{M}'}^{\perp}
=
g_{\mathcal{M}'}-g_{\mathcal{M}'}^{\parallel}
$$

为**最小化对梯度方向的调整**，保留平行分量，同时按比例**缩小正交分量**。设 $$\theta=\arccos\tau$$，则锥边界处的**最大横向比率**为：

$$
\kappa_{\max}
=
\tan\theta
=
\frac{\sqrt{1-\tau^{2}}}{\tau}
$$

相应的**缩放因子**为：

$$
\alpha
=
\kappa_{\max}
\frac{\left\|g_{\mathcal{M}'}^{\parallel}\right\|_2}
{\left\|g_{\mathcal{M}'}^{\perp}\right\|_2}
$$

此处 $$\alpha<1$$ 确保正交分量被缩减以**适配锥边界**。调制后的梯度为：

$$
\tilde{g}_{\mathcal{M}'}
\leftarrow
g_{\mathcal{M}'}^{\parallel}
+
\alpha g_{\mathcal{M}'}^{\perp}
$$

由构造可知：

$$
\mathrm{sim}\left(\tilde{g}_{\mathcal{M}'},g_{\mathcal{A}}\right)=\tau
$$

即调制后的梯度**恰好落在安全锥边界上**，在限制梯度偏差的同时**保留了锚点平行分量**。

##### （3）与锚点相反：直接抑制

当 $$\mathrm{sim}(g_{\mathcal{M}'},g_{\mathcal{A}})<0$$ 时，重建梯度相对锚点产生**冲突的优化信号**，可能导致优化漂移。因此直接**置零**：

$$
\tilde{g}_{\mathcal{M}'}\leftarrow 0
$$

##### （4）参数更新

得到对齐后的重建梯度后，模型参数使用**聚合梯度**更新：

$$
\theta
\leftarrow
\theta-\eta\left(g_{\mathcal{C}}+\tilde{g}_{\mathcal{M}'}\right)
$$

其中 $$\eta$$ 为**优化器学习率**。

> 注意：完整样本梯度 $$g_{\mathcal{C}}$$ **始终原样参与更新**，被调制的只有锚点集之外的重建梯度，这正是"以完整样本为基准重新平衡优化"的体现。

#### 3.4 语义增强适配器（SEA）

优化层面的平衡不能解决**语义不足**问题。为显式丰富上下文知识、增强缺失场景下的鲁棒性，作者提出**语义增强适配器（Semantic-Enhanced Adapter，SEA）**。

对每个目标实例（**包括完整样本和重建样本**），SEA从检索池 $$\mathcal{N}_i$$ 生成**动态、上下文感知的提示**。

具体地，给定目标文本序列 $$\mathcal{T}_i\in\mathbb{R}^{n_t\times d}$$，SEA执行**交叉注意力**：$$\mathcal{T}_i$$ 作为**查询**，Top-$$K$$检索到的文本序列作为**键和值**：

$$
\mathcal{T}_i^{R}
=
\left\{\mathcal{T}_i^{r_j}\right\}_{j=1}^{K}
\in\mathbb{R}^{K\times n_t\times d}
$$

该操作生成**文本感知提示** $$\tilde{P}_i^{t}\in\mathbb{R}^{n_t\times d}$$：

$$
\tilde{P}_i^{t}
=
\mathrm{CrossAttn}
\left(
f_t^{Q}(\mathcal{T}_i),
f_t^{K}(\mathcal{T}_i^{R}),
f_t^{V}(\mathcal{T}_i^{R})
\right)
$$

$$
=
\mathrm{softmax}
\left(
\frac{
f_t^{Q}(\mathcal{T}_i)
f_t^{K}(\mathcal{T}_i^{R})^{\top}
}{\sqrt{d}}
\right)
f_t^{V}(\mathcal{T}_i^{R})
$$

其中 $$f_t^{Q}(\cdot)$$、$$f_t^{K}(\cdot)$$、$$f_t^{V}(\cdot)$$ 为各自的投影函数（例如线性层），$$\sqrt{d}$$ 为点积注意力的缩放因子。

对于**视觉感知提示**，SEA在目标图像序列 $$\mathcal{V}_i\in\mathbb{R}^{n_v\times d}$$ 与其检索对应序列 $$\mathcal{V}_i^{R}\in\mathbb{R}^{K\times n_v\times d}$$ 之间执行相同操作，生成 $$\tilde{P}_i^{v}\in\mathbb{R}^{n_v\times d}$$。

最后通过**自适应池化**为每个目标实例生成动态提示：

$$
P_i^{t},P_i^{v}\in\mathbb{R}^{d}
$$

并将其**注入多模态Transformer的MSA层**中。

> 与静态、输入无关的提示相比，SEA的提示是**实例特定**的，能够携带检索实例中的上下文语义。

#### 3.5 方法特点小结

- ANGA不改变重建器本身，而是**在优化层面**解决重建噪声导致的学习不平衡；
- 锚点是**动态**的：随课程推进，越来越多的低熵重建样本被纳入锚点；
- 梯度调制是**方向性**的（只改方向不改"是否更新"的粗粒度开关），且只影响锚点外的重建梯度；
- 该策略**可无缝集成**到已有的不完全多模态学习框架中。



### 4. 实验

#### 4.1 实验设置

**数据集：**

| 数据集    | 任务             | 规模                 | 指标     |
| --------- | ---------------- | -------------------- | -------- |
| HateMemes | 仇恨表情包检测   | 10,000 图文对        | AUROC    |
| MM-IMDb   | 电影类型分类（多标签） | 25,959 图文对   | F1-Micro |
| Food-101  | 食物分类（101类） | 90,688 图文对       | ACC      |

**对比方法（三类SOTA基线）：**

- **模态不变学习方法：** IF-MMIN、ShaSpec、DrFuse、CorrKD、MoMKE；
- **基于VLP的方法：** ViLT、MAPs、MSPs、IPer；
- **模态重建方法（生成式与检索式）：** AcMAE、DiCMoR、MMDS、RAGPT。

**缺失模式设置：** 缺失率 $$\delta$$ 定义为具有缺失模态的样本占整个数据集的比例，考虑两种广泛使用的模式：

1. **单模态缺失：** $$\delta$$ 比例的样本为纯图像或纯文本，剩余 $$(1-\delta)$$ 完整；
2. **双模态缺失：** $$\frac{\delta}{2}$$ 为纯图像，$$\frac{\delta}{2}$$ 为纯文本，剩余 $$(1-\delta)$$ 完整。

除非另有说明，所有实验中 $$\delta=70\%$$。

**实现细节：**

- 骨干网络：预训练 **ViLT**；
- 记忆库 $$\mathcal{M}$$ 由相应训练集构建，检索实例数 $$K\in\{1,3,5,7,9\}$$；
- 锚点构建：$$Z_{grow}=5$$，$$\lambda_{\min}=0.1$$，$$\lambda_{\max}=0.3$$；
- 提示插入 ViLT 的**第二 MSA 层**（$$b=2$$）；
- 余弦阈值 $$\tau=0.2$$；
- AdamW，学习率 $$\eta=10^{-3}$$，权重衰减 $$10^{-5}$$，批量大小 64；
- 单卡 NVIDIA GeForce RTX 4090。

#### 4.2 与SOTA基线的比较

![ANGA-table1](./assets/ANGA-table1.png)

主要观察：

1. **模态不变学习方法**过度强调共享表示而忽视模态特定信息，削弱了判别性能；**基于VLP的方法**普遍优于原始Transformer（ViLT），验证了提示学习的价值，但其提示**输入无关**，无法融入实例特定知识；
2. **模态重建方法**通过生成或检索恢复缺失模态，但引入语义噪声、加剧优化漂移，在高缺失率下导致学习不平衡；ANGA构建优化锚点使重建样本梯度与完整样本梯度对齐，有效抑制漂移；
3. 相比最佳基线 **RAGPT**，ANGA在文本缺失 / 图像缺失 / 双缺失场景下：
   - **AUROC 分别提升 4.44% / 0.85% / 1.65%**；
   - **ACC 分别提升 1.70% / 1.05% / 1.53%**。

#### 4.3 消融研究

<img src="./assets/ANGA-table2.png" alt="ANGA-table2" style="zoom:67%;" />

70%文本缺失条件下各组件（MIR / GA / SEA）的消融：

| MIR | GA | SEA | HateMemes AUROC (%) | MM-IMDb F1-Micro (%) | Food101 ACC (%) |
| :-: | :-: | :-: | ------------------: | -------------------: | --------------: |
| ✗ | ✗ | ✗ | 59.38 | 49.62 | 68.53 |
| ✓ | ✗ | ✗ | 64.63 | 54.17 | 74.86 |
| ✓ | ✓ | ✗ | 66.82 | 56.32 | 77.01 |
| ✓ | ✗ | ✓ | 67.31 | 55.74 | 76.89 |
| ✓ | ✓ | ✓ | **68.54** | **57.28** | **77.23** |

结论：

1. **不使用MIR**时缺失模态被替换为**虚拟值**，导致严重信息缺失，所有数据集上表现最差；引入MIR带来显著提升（HateMemes 上 **+5.25% AUROC**）；
2. 在MIR基础上，**梯度对齐（GA）**缓解语义噪声引起的优化漂移，维持完整样本与重建样本之间的平衡学习，进一步带来 **+2.19% AUROC**（HateMemes）；
3. **语义增强适配器（SEA）**从检索样本生成动态提示，让模型融入实例相关的上下文知识，进一步提升鲁棒性；
4. 三者**互补**，整合后取得最佳整体性能。

#### 4.4 进一步分析

##### （1）超参数敏感性

<img src="./assets/ANGA-fig3.png" alt="ANGA-fig3" style="zoom:75%;" />

两个关键超参数：检索实例数 $$K$$ 与定义锥形区域的余弦阈值 $$\tau$$。

- ANGA 的性能**仅受 $$K$$ 的轻微影响**，对检索基数不敏感；尽管存在波动，仍**始终优于虚拟填充基线**（消融表中的变体1）；
- 随着 $$\tau$$ 增加，性能**先提升后下降**。说明**适度大小的锥形区域**有助于缓解学习不平衡，但**过度考虑梯度对齐会阻碍模型泛化**。

##### （2）对不同缺失率的鲁棒性

<img src="./assets/ANGA-fig4.png" alt="ANGA-fig4" style="zoom:75%;" />

在HateMemes上比较 IF-MMIN、MSPs、RAGPT：

- 在 10%–90% 的广泛缺失率范围内，ANGA **始终取得最高AUROC**；
- 相对于完整模态条件的**相对性能退化**，ANGA 在**每个缺失率下都最小**。

这说明ANGA利用检索实例引导的动态提示，有效缓解了缺失信息的影响。

##### （3）对严重缺失条件的泛化能力

<img src="./assets/ANGA-fig5.png" alt="ANGA-fig5" style="zoom:75%;" />

用 **10%–50%** 的训练缺失率训练，在 **90%** 缺失率下测试：

- 增加训练期间的缺失率，**所有模型**在文本缺失和双缺失场景下的性能都持续提升，说明**接触更多不完整数据能增强对推理阶段严重缺失的泛化能力**；
- ANGA 取得最佳整体性能，得益于其梯度对齐策略在高缺失率下促进了稳定优化。

##### （4）可视化分析

<img src="./assets/ANGA-fig6.png" alt="ANGA-fig6" style="zoom:75%;" />

在70%缺失条件下进行两类t-SNE可视化：

- **重建可视化：** 从HateMemes随机选取500个测试样本，观察到**重建的文本与图像嵌入仍明显偏离其真实对应项**，揭示了通用检索式重建范式（如RAGPT）下存在明显的**语义鸿沟（modality gap）**——这正是学习不平衡的根源；
- **决策空间可视化：** MM-IMDb 中运动、惊悚、音乐三类的最终层嵌入，**ANGA之前**类别可分离性差、表示纠缠，**ANGA之后**形成紧凑且分离良好的簇。

##### （5）效率比较

<img src="./assets/ANGA-fig7.png" alt="ANGA-fig7" style="zoom:75%;" />

在HateMemes上分析可学习参数量：ANGA 在保持**具有竞争力的参数效率**的同时实现了最佳性能。



### 5. 局限性与未来工作

#### 5.1 局限性

1. **仍依赖重建器的质量。** ANGA只在优化层面纠偏，可视化结果显示检索式重建的嵌入与真实模态之间**语义鸿沟依然存在**，重建质量本身没有被改善。
2. **锥形阈值需要人工调节且敏感。** 实验表明 $$\tau$$ 过大反而损害泛化，最优值需按数据集调参（原文取0.2）。
3. **课程超参为固定线性调度。** $$\lambda_{\min}$$、$$\lambda_{\max}$$、$$Z_{grow}$$ 是人工设定的固定值，未随任务或缺失程度自适应。
4. **可靠性判据依赖预测熵。** 低熵不等于正确，模型可能对错误类别高置信，从而把语义错位的重建样本纳入锚点。
5. **训练开销增加。** 需要在每个小批量内**分别计算**完整样本、重建样本与锚点的梯度，反向传播次数增多。
6. **仅验证于双模态图文分类任务。** 实验限于两模态（图像/文本）的分类基准，未在三模态及以上、或检测/分割/生成等任务上验证。

#### 5.2 未来工作

- 结合更强的模态重建（生成式）方法，从源头缩小语义鸿沟；
- 学习自适应的锥形阈值与课程调度，而非固定超参；
- 引入更可靠的重建样本筛选准则（如不确定性估计、原型一致性）替代单纯的预测熵；
- 将梯度对齐推广到**三模态及以上**、以及分割、检测、序列生成等更广泛的任务；
- **将该优化平衡视角与"模态不平衡"研究统一，探索隐式与显式不平衡的联合处理**。



### 6. 总结

ANGA关注的不是"如何把缺失模态重建得更好"，而是：

> **重建完成后，如何让重建样本的优化信号不去破坏完整样本学到的表示？**

其核心流程可以概括为：

1. 用多模态实例检索器从记忆库检索Top-$$K$$相似实例，重建缺失模态，缓解信息缺失；
2. 在每个小批量内**解耦**完整样本梯度 $$g_{\mathcal{C}}$$ 与重建样本梯度 $$g_{\mathcal{M}}$$；
3. 用**预测熵**衡量重建样本可靠性，按**熵驱动课程**逐步把低熵重建样本与完整样本合并为**优化锚点**；
4. 以锚点梯度 $$g_{\mathcal{A}}$$ 为方向参考，对锚点外的重建梯度做**锥形三分支调制**：锥内保留、锥外正相关则投影到锥边界、与锚点相反则置零；
5. 用 $$g_{\mathcal{C}}+\tilde{g}_{\mathcal{M}'}$$ 更新参数，重新平衡两类样本的学习；
6. 用**语义增强适配器**从检索实例生成动态提示注入MSA层，补充上下文语义。

ANGA把传统的：

$$
\text{重建缺失模态}
\quad\longrightarrow\quad
\text{全部样本一起优化}
$$

转化为：

$$
\text{重建缺失模态}
+
\text{可靠性课程筛选}
+
\text{以完整样本为锚的梯度对齐}
$$

从而在利用重建信息的同时，抑制语义噪声造成的优化漂移，缓解高缺失率下的学习不平衡。
